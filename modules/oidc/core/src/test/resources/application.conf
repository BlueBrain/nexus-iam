include "app.conf"

app.description.name = "iam-oidc-core"

akka {
  log-dead-letters = on
  log-dead-letters-during-shutdown = on
  loglevel = debug

  remote.netty.tcp.port = 0

  test {
    timefactor = 3.0
  }
}